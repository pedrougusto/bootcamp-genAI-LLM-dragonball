# 🐉 Dragon Ball GenAI — LLM with Custom Knowledge Base

> **Bootcamp project:** Building a domain-specific LLM assistant grounded on Dragon Ball universe lore using Retrieval-Augmented Generation (RAG).

---

## Overview

This project was developed as part of a **Generative AI Bootcamp** and explores how to build a conversational AI assistant that answers questions about the Dragon Ball universe with accuracy and context.

Instead of relying solely on a general-purpose LLM's training data, the pipeline ingests structured knowledge sources about Dragon Ball's story, characters, sagas, and lore — grounding the model's responses in a curated knowledge base.

---

## Architecture

```
┌─────────────────────────────────────────────────────┐
│                   KNOWLEDGE SOURCES                  │
│   Dragon Ball lore · Character bios · Saga summaries │
└──────────────────────────┬──────────────────────────┘
                           │
              ┌────────────▼────────────┐
              │     Document Ingestion   │
              │   Chunking + Embedding   │
              └────────────┬────────────┘
                           │
              ┌────────────▼────────────┐
              │       Vector Store       │
              │  Semantic similarity     │
              │        retrieval         │
              └────────────┬────────────┘
                           │
              ┌────────────▼────────────┐
              │        LLM Layer         │
              │  Context-aware response  │
              │       generation         │
              └────────────┬────────────┘
                           │
              ┌────────────▼────────────┐
              │     Conversational UI    │
              │   Q&A about Dragon Ball  │
              └─────────────────────────┘
```

---

## What it does

The assistant answers questions grounded in Dragon Ball lore, such as:

- *"What are the transformations of Goku and what triggers each one?"*
- *"Summarize the Frieza Saga and its key battles."*
- *"Who is the strongest character in Dragon Ball Super and why?"*
- *"What is the difference between Dragon Ball Z and Dragon Ball GT?"*

Responses are generated by retrieving the most relevant passages from the knowledge base and passing them as context to the language model — a classic **RAG (Retrieval-Augmented Generation)** pattern.

---

## Tech Stack

| Component | Technology |
|---|---|
| **LLM** | Gemini / OpenAI GPT |
| **Embeddings** | Google Generative AI Embeddings |
| **Vector Store** | FAISS / Chroma |
| **Orchestration** | LangChain / NotebookLM |
| **Notebook** | Google Colab |
| **Knowledge Base** | Custom Dragon Ball lore documents |

---

## Key Concepts Demonstrated

**RAG (Retrieval-Augmented Generation)**
Rather than fine-tuning the model, relevant knowledge is retrieved at query time and injected into the prompt. This allows the LLM to answer accurately about a niche domain without retraining.

**Document Chunking**
Source documents are split into semantically coherent chunks before embedding, balancing retrieval precision with context richness.

**Semantic Search**
User questions are embedded and matched against the knowledge base using cosine similarity, ensuring the most relevant passages are retrieved even when the wording differs from the source.

**Prompt Engineering**
The system prompt instructs the model to answer only based on retrieved context, reducing hallucination and keeping responses grounded in Dragon Ball canon.

---

## Project Structure

```
bootcamp-genAI-LLM-DragonBall/
├── notebooks/
│   └── dragonball_rag_pipeline.ipynb   # Main notebook
├── data/
│   └── dragonball_lore/                # Knowledge base documents
├── src/
│   ├── ingestion.py                    # Document loading & chunking
│   ├── embeddings.py                   # Embedding generation
│   └── retrieval.py                    # Semantic search & RAG chain
└── README.md
```

---

## Example Interaction

```
User:  Who is stronger, Goku or Vegeta?

Model: Based on the Dragon Ball lore, both warriors have achieved
       comparable power levels across different sagas. In Dragon Ball Super,
       both reached Ultra Instinct and Super Saiyan Blue Evolved respectively.
       However, Goku has more consistently pushed beyond his limits in
       critical battles, suggesting a slight edge — though Vegeta's
       rivalry drives continuous evolution in both fighters.
       [Source: Dragon Ball Super — Tournament of Power arc]
```

---

## What I learned

- How to structure a **RAG pipeline** end-to-end: ingestion → chunking → embedding → retrieval → generation
- The importance of **chunk size tuning** — too small loses context, too large dilutes relevance
- How **prompt engineering** controls hallucination in domain-specific assistants
- Practical use of **vector similarity search** for semantic retrieval

---

## About this project

Developed during the **Generative AI Bootcamp**, this project demonstrates how GenAI techniques can be applied to any knowledge domain — the Dragon Ball theme makes the concepts approachable and fun to demonstrate, while the underlying architecture is directly transferable to real-world use cases like internal knowledge bases, customer support bots, and document Q&A systems.

---

*Part of [Pedro Augusto's Data & AI Portfolio](https://github.com/YOUR_USERNAME)*

---
---

# 🐉 Dragon Ball GenAI — LLM com Base de Conhecimento Customizada

> **Projeto de Bootcamp:** Construção de um assistente LLM especializado no universo Dragon Ball usando Retrieval-Augmented Generation (RAG).

---

## Visão Geral

Este projeto foi desenvolvido durante um **Bootcamp de Inteligência Artificial Generativa** e explora como construir um assistente conversacional capaz de responder perguntas sobre o universo Dragon Ball com precisão e contexto.

Em vez de depender apenas do conhecimento de treinamento de um LLM genérico, o pipeline ingere fontes estruturadas sobre a história, personagens, sagas e lore do Dragon Ball — ancorando as respostas do modelo em uma base de conhecimento curada.

---

## Arquitetura

```
┌─────────────────────────────────────────────────────┐
│                  FONTES DE CONHECIMENTO              │
│   Lore Dragon Ball · Fichas de personagens · Sagas   │
└──────────────────────────┬──────────────────────────┘
                           │
              ┌────────────▼────────────┐
              │    Ingestão de Docs      │
              │  Chunking + Embeddings   │
              └────────────┬────────────┘
                           │
              ┌────────────▼────────────┐
              │      Vector Store        │
              │  Busca por similaridade  │
              │        semântica         │
              └────────────┬────────────┘
                           │
              ┌────────────▼────────────┐
              │       Camada LLM         │
              │  Geração de resposta     │
              │   com contexto           │
              └────────────┬────────────┘
                           │
              ┌────────────▼────────────┐
              │       Interface Q&A      │
              │  Perguntas sobre DBZ     │
              └─────────────────────────┘
```

---

## O que o projeto faz

O assistente responde perguntas ancoradas no lore do Dragon Ball, como:

- *"Quais são as transformações do Goku e o que desencadeia cada uma?"*
- *"Resuma a Saga de Freeza e suas batalhas principais."*
- *"Qual é o personagem mais forte no Dragon Ball Super e por quê?"*
- *"Qual a diferença entre Dragon Ball Z e Dragon Ball GT?"*

As respostas são geradas recuperando as passagens mais relevantes da base de conhecimento e passando-as como contexto ao modelo de linguagem — o padrão clássico de **RAG (Retrieval-Augmented Generation)**.

---

## Stack

| Componente | Tecnologia |
|---|---|
| **LLM** | Gemini / OpenAI GPT |
| **Embeddings** | Google Generative AI Embeddings |
| **Vector Store** | FAISS / Chroma |
| **Orquestração** | LangChain / NotebookLM |
| **Notebook** | Google Colab |
| **Base de Conhecimento** | Documentos customizados sobre Dragon Ball |

---

## Conceitos Demonstrados

**RAG (Retrieval-Augmented Generation)**
Em vez de fazer fine-tuning no modelo, o conhecimento relevante é recuperado em tempo de consulta e injetado no prompt. Isso permite ao LLM responder com precisão sobre um domínio específico sem retreinamento.

**Chunking de Documentos**
Os documentos fonte são divididos em partes semanticamente coerentes antes do embedding, equilibrando precisão na recuperação com riqueza de contexto.

**Busca Semântica**
As perguntas do usuário são transformadas em embeddings e comparadas com a base de conhecimento por similaridade de cosseno — garantindo que as passagens mais relevantes sejam recuperadas mesmo quando a formulação difere da fonte.

**Engenharia de Prompt**
O system prompt instrui o modelo a responder apenas com base no contexto recuperado, reduzindo alucinações e mantendo as respostas fiéis ao cânone Dragon Ball.

---

## Estrutura do Projeto

```
bootcamp-genAI-LLM-DragonBall/
├── notebooks/
│   └── dragonball_rag_pipeline.ipynb   # Notebook principal
├── data/
│   └── dragonball_lore/                # Documentos da base de conhecimento
├── src/
│   ├── ingestion.py                    # Carregamento e chunking de documentos
│   ├── embeddings.py                   # Geração de embeddings
│   └── retrieval.py                    # Busca semântica e cadeia RAG
└── README.md
```

---

## O que aprendi

- Como estruturar um **pipeline RAG** de ponta a ponta: ingestão → chunking → embedding → recuperação → geração
- A importância do **ajuste do tamanho do chunk** — muito pequeno perde contexto, muito grande dilui a relevância
- Como **engenharia de prompt** controla alucinações em assistentes de domínio específico
- Uso prático de **busca vetorial por similaridade semântica**

---

## Sobre este projeto

Desenvolvido durante o **Bootcamp de Inteligência Artificial Generativa**, este projeto demonstra como técnicas de GenAI podem ser aplicadas a qualquer domínio de conhecimento. O tema Dragon Ball torna os conceitos acessíveis e divertidos de demonstrar, mas a arquitetura subjacente é diretamente transferível para casos de uso reais como bases de conhecimento internas, bots de suporte ao cliente e sistemas de Q&A sobre documentos.

---

*Parte do [Portfólio de Dados & IA do Pedro Augusto](https://github.com/YOUR_USERNAME)*
